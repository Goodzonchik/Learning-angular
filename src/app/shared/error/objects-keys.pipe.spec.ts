import { ObjectsKeysPipe } from './objects-keys.pipe';

describe('ObjectsKeysPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectsKeysPipe();
    expect(pipe).toBeTruthy();
  });
});
