import { ListButtonDirective } from './list-button.directive';

describe('ListButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ListButtonDirective();
    expect(directive).toBeTruthy();
  });
});
